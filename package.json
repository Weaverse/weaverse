{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --force-local && lerna link && lerna run build",
    "patch": "lerna version patch"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "0.1.4"
}
