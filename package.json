{
  "name": "root",
  "private": true,
  "devDependencies": {
    "turbo": "*"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev2": "turbo run dev --no-cache",
    "test": "turbo run test",
    "build": "turbo run build --graph=repo-graph.svg"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "packageManager": "pnpm@6.31.0"
}
