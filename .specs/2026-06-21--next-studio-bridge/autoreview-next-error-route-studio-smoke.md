autoreview clean: no accepted/actionable findings reported
overall: patch is correct (0.95)
The patch is a clean, low-risk refactor that extracts the inline `useEffect` script-loading logic from `WeaverseNextStudioConnect` into a standalone `loadWeaverseNextStudioScript` helper, then delegates from the component to that helper. The extracted helper is functionally identical to the original inline code: it falls back to `window.location` when no context is supplied, resolves `storefrontHostname` from options or `window.location.hostname`, and calls the pre-existing `loadScript` (which is already SSR-safe via `typeof document !== 'undefined'`). No behavioural regression is introduced for existing consumers. The five new unit tests are well-isolated: each uses a fresh `document` stub via `vi.stubGlobal`, and the design-mode, preview-mode, dedupe, SSR, and untrusted-host tests each use distinct `weaverseVersion` query-string values to avoid collisions in the module-level `loadedScriptSrcs` set across test runs. The new exports are additive and correctly placed in `index.ts`. The README addition and limitations-list update accurately reflect the new coverage. No security, correctness, or regression issues are introduced by the diff.
