{
  "version": "0.3.1",
  "license": "MIT",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "type": "commonjs",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "tsup src/index.ts --format esm,cjs --dts --outDir dist --watch --external react",
    "build": "tsup src/index.ts --format esm,cjs --dts --outDir dist --external react"
  },
  "peerDependencies": {
    "react": ">=17",
    "react-dom": ">=17"
  },
  "prettier": {
    "printWidth": 80,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "name": "@weaverse/react",
  "author": "Weaverse Team",
  "dependencies": {
    "@weaverse/core": "^0.3.0"
  }
}
